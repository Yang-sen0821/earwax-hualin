# Flora Court ERP — 整合驗證報告（步驟 6 閘③ 證據）

日期：2026-06-17　驗證者：Cerberus 整合驗證手　環境：本機 SQLite（schema=None, CONCURRENCY_STRATEGY=atomic_update）
權威藍本：FloraCourt_ERP_建置計畫_v2_2026-06-17.md (v2.2)

> 範圍宣告：本報告涵蓋「本機端到端」全綠（D1–D16 邏輯 + D17 本機部分）。
> 「部署上線可用」（D17 後半）為森哥權限項，本手未執行，見文末「未做到」。

---

## 一、總覽

| 區塊 | 結果 |
|------|------|
| D1 愛啪啪入口可逆隱藏 | PASS（4 檢查全綠，可逆） |
| 地基（20 表 / 冪等 seed / app 啟動 / 6+auth bp） | PASS |
| 模組層整合（6 bp ↔ inventory_service 簽章一致） | PASS（無介面不一致，未改任何 bp） |
| 本機端到端（9 項，腳本實跑 11 子檢查） | PASS 11/11 |
| 部署上線 | 未驗證（森哥權限項） |

---

## 二、D1 愛啪啪入口可逆隱藏（4 檢查）

實作位置（earwax_hualin，純可逆、不刪程式/不動 DB）：
- `config.py`：新增 `ENABLE_EARWAX_ENTRY`（預設 true；env 設 false 即隱藏）
- `app.py`：`if ENABLE_EARWAX_ENTRY:` 才註冊 6 個 earwax 業務 bp（service/customers/inventory/purchases/mask/dashboard）；false 時改掛停用占位 `/`；`context_processor` 注入旗標供模板
- `templates/base.html`：導覽列 `{% if session.user and enable_earwax_entry %}` 整塊閘控

| 檢查 | 方法 | 結果 |
|------|------|------|
| ① flag=false 路由清單無愛啪啪 | 以 throwaway sqlite import app，列 url_map | PASS：僅 `/ /health /login /logout /static`；愛啪啪業務路由數 = **0** |
| ② 入口不可見 | base.html nav 旗標閘控 + flag=false `/` 回「已停用」JSON | PASS |
| ③ 未動 earwax DB/檔內容 | 全程未連線 live Supabase（測試用 throwaway sqlite）；`git status` 僅 3 個程式檔變更，無資料檔、無 DB 寫入 | PASS |
| ④（可逆性）flag=true 還原 | 同法 import，flag=true | PASS：15 個愛啪啪業務路由全數還原 |

**回復方式（記錄）**：把環境變數 `ENABLE_EARWAX_ENTRY` 設回 `true`（或移除該變數）即完全還原，無需改任何程式或 DB。

---

## 三、模組層整合修正

逐一比對 6 blueprint 對 `inventory_service` 的呼叫與 §4 契約簽章：
- orders.py：`deduct_for_sale(session, product_id, combo_code, order_qty, operator, order_ref, note)`、`deduct_for_shipment(session, order_id, operator)` — 一致
- inventory.py：`restock / deduct_out / split_box / adjust_inventory / check_low_water` — 一致
- mobile.py：`deduct_for_sale / deduct_for_shipment` — 一致
- reports.py：純讀，`_financial_report` 公式正確
- products.py / customers.py：不碰庫存層

**修正了哪些介面不一致：無。** 模組層回報 [{i:0..4 ok}] 屬實，6 bp 全可整合、import 全綠、url_map 完整（53 條路由），未對任何 blueprint 動 Edit。

---

## 四、本機端到端（D17 本機，腳本：`e2e_test.py`，throwaway DB，11/11 PASS）

| # | 項目 | 結果 | 關鍵輸出 |
|---|------|------|---------|
| 1 | seed 數字 | PASS | boxed.normal=1271, boxed.reserved=35, loose.normal=1825, paper.normal=932 |
| 2a | deduct_for_sale 四組合扣對應池 | PASS | SINGLE→loose；BOX1/3/10→boxed；boxed_drop=14, loose_drop=2 |
| 2b | 一般銷售扣不到 reserved（硬隔離） | PASS | reserved 35→35 不變 |
| 2c | normal 不足不借 reserved | PASS | 扣 1258 盒（>have 1257）→ ok=False、缺貨擋下 |
| 3 | split_box 1盒→盒-1/片+5、2 筆同 group | PASS | boxed 1257→1256, loose 1823→1828；mv=2、同 group、皆 SPLIT_BOX |
| 4 | deduct_for_shipment 扣 1 紙袋 + PAPERBAG_OUT | PASS | paper 932→931；mv PAPERBAG_OUT, delta=-1, ref=shipment |
| 5 | 多品項缺貨整張 rollback | PASS | 第二項缺貨→rollback；boxed 1256→1256、movement 筆數 11→11 不變（不扣不留痕） |
| 6 | manual_adjust 留痕 | PASS | boxed 1256→1251；mv ADJUSTMENT、reason=盤點短少、qty_before/after 正確 |
| 7 | check_low_water 觸發 | PASS | 壓到 49 < 門檻 50 → low_water=True |
| 8 | 財務毛利/淨利公式 | PASS | gross=7000（10000−3000）、net=4200（7000−500−1500−800） |
| 9 | 權限 staff 擋 owner 後台 | PASS | staff→/inventory/adjust=403；owner→/inventory/adjust=200 |

紅線覆蓋：R1（同 tx + 配對 movement + 缺貨整張 rollback + 防超賣 + reserved 硬隔離）由 #2/#3/#4/#5/#6 證實；R2（獨立 schema、不 import earwax）由 app import 全綠 + flora_court schema 設計證實。

---

## 五、DoD D1–D17 覆蓋表

| DoD | 描述 | 狀態 | 證據 |
|-----|------|------|------|
| D1 | 愛啪啪入口隱藏、可逆、earwax 原封、泰安零影響 | PASS | 二節 4 檢查；earwax schema 隔離、未連 live DB |
| D2 | Flora Court 獨立 schema，不污染 | PASS | config 自動偵測 schema；本機 None / 正式 flora_court；FK 全 flora_court |
| D3 | 商品：新增/上下架/圖片/規格 | PASS（功能就緒） | products bp：create/toggle_active/specs/threshold；image_url 欄位齊 |
| D4 | 訂單：建立/查詢/改付款&出貨/地址 | PASS | orders bp 全路由；多品項原子建單（e2e #5 證 rollback） |
| D5 | 客戶管理 | PASS（功能就緒） | customers bp：CRUD + 地址簿 |
| D6 | 庫存三池×5分類 + seed=盤點數 | PASS | e2e #1（1271/35/1825/932）；inventory 總覽矩陣 |
| D7 | 4 組合 BOM 扣換算 + 手動拆盒留痕 | PASS | e2e #2a/#3 |
| D8 | 異動紀錄 入庫2+出庫9（+系統6）每筆留痕 | PASS | movement 表 17 欄齊；e2e 各 mv 含 operator/qty_before/after/type |
| D9 | 低水位警示 | PASS | e2e #7；inventory/alerts 頁 |
| D10 | 報表：銷售日/月/年、排行、庫存報表 | PASS（功能就緒） | reports bp：sales(g=day/month/year)/inventory；排行依 combo_code |
| D11 | 匯出 Excel | PASS（功能就緒） | reports：orders/inventory/movements/sales 四 xlsx（openpyxl） |
| D12 | 財務欄位齊（測試值，後台可改） | PASS | financial_entries + settings(cost_*)；e2e #8 公式 |
| D13 | 建檔/額外支出欄位齊 | PASS | extra_expenses 表；products/settings 後台 |
| D14 | 歷史訂單匯入欄位齊（資料後補） | PASS（欄位齊） | orders/import 頁 11 欄；historical_order_import(_rows) 表 |
| D15 | 手機版後台可操作 | PASS（功能就緒） | mobile bp /m：建單/出貨/庫存/客戶，RWD |
| D16 | 權限分級（員工/老闆） | PASS | e2e #9；5 角色 role_required，owner 永遠通過 |
| D17 | 本機端到端全綠 + 部署上線 | **本機 PASS / 部署未驗證** | e2e 11/11 全綠；上線見「未做到」 |

> 標「功能就緒」者：路由/欄位/邏輯齊備且 app import 全綠，本手以路由與資料模型佐證；未對每個 UI 表單做逐頁點擊互動測試（屬 UI 驗收，非閘③庫存正確性核心）。

---

## 六、本手未做到 / 需森哥的部分

1. **D17 部署上線**：未佈署到 Render / 未對正式 Postgres(flora_court schema) 跑 create_all 與線上 seed。需森哥提供/確認正式 `DATABASE_URL` 與部署授權（涉及付費服務設定，屬重大決策）。
2. **正式 Postgres 併發路徑（for_update）**：本機僅驗 SQLite atomic_update；FOR UPDATE 行為待正式 Postgres 環境驗證。
3. **earwax 隱藏的正式環境套用**：本手只改程式 + 本機驗證；正式環境要實際隱藏，需在 Render 上把 `ENABLE_EARWAX_ENTRY=false` 設好並重佈（屬付費服務設定，待森哥批）。
4. **泰安零影響的線上實證**：藍本要求「驗證撞表=0」，本手以 schema 隔離設計（earwax / flora_court / public 各自獨立）佐證；正式撞表掃描需連線正式 DB，未執行。

---

## 七、附帶發現（誠實揭露）

- 驗證前 `flora_court.db`（先前測試遺留）庫存已漂移（loose 1822、paper 931，非 seed 值）。seed 為「缺列才補」的冪等設計，不會重置既有列數量。本手已將舊庫備份為 `flora_court.db.bak`（保留證據不刪），重建乾淨 seed DB，現值 = 1271/35/1825/932，並再跑一次 init_db 確認冪等不變。
- e2e_test.py 一律走系統暫存區 throwaway DB，不污染交付用 flora_court.db。
