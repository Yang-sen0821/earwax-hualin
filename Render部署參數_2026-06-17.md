# Flora Court 面膜 ERP — Render 免費版部署參數表

日期：2026-06-17　對象：森哥照填即可（非工程師友善版）
本機 commit：`1459e0c`（尚未 push，遠端 repo 建好後才連）

---

## 步驟 0：先把程式碼放上 GitHub（一次性）

Render 是「從 GitHub 拉程式碼」來跑的。所以順序是：
1. 在 GitHub 建一個新的空 repo（建議設 Private 私有，因為這是 ERP）
2. 把本機已 commit 好的程式碼 push 上去（這一步等遠端 repo 網址出來後，執行手會幫你做）
3. 到 Render 連這個 repo（下方表格）

> 機密安全：`.env`、資料庫檔（`*.db`）都已被 `.gitignore` 擋住，不會上傳到 GitHub。
> 真正的密碼/連線字串只會手動填進 Render 的 Environment Variables（見下方），不寫進程式碼。

---

## 步驟 1：Render 建立 Web Service

Render 後台 → New → Web Service → 連到上面那個 GitHub repo，然後照填：

| 欄位 | 填什麼 | 說明 |
|------|--------|------|
| Repository | （待 push 後選你的 flora_court repo） | 連 GitHub |
| Branch | `main` | 主分支 |
| Root Directory | （留空） | 程式就在 repo 根目錄 |
| Runtime / Language | `Python 3` | |
| Build Command | `pip install -r requirements.txt` | 安裝套件 |
| Start Command | `gunicorn app:app` | 用 gunicorn 跑 Flask（`app.py` 裡有 `app` 物件） |
| Instance Type | `Free` | 免費版 |

> `requirements.txt` 已內含 `gunicorn`，不需另外加。

---

## 步驟 2：Environment Variables（環境變數）

Render 後台 → 你的 service → Environment → Add Environment Variable，逐筆加入：

| Key | Value（值） | 來源說明 |
|-----|------------|---------|
| `DATABASE_URL` | （從 .env 複製） | **同 flora_court/.env 第 8 行，森哥從 .env 複製貼上，勿寫進本文件**。Supabase Postgres pooler 連線字串 |
| `DB_SCHEMA` | `flora_court` | 固定值，獨立 schema（紅線 R2，禁碰 public/earwax/泰安） |
| `SECRET_KEY` | （從 .env 複製） | **同 flora_court/.env 第 5 行，森哥從 .env 複製貼上，勿寫進本文件**。Flask session 加密金鑰 |
| `ENABLE_MOBILE` | `true` | 開啟手機後台 `/m`（預設就是 true，可不填） |
| `ENABLE_EARWAX_ENTRY` | `false` | 愛啪啪入口維持隱藏（預設 false，可不填） |

> 程式運作原理：`config.py` 直接讀 `os.environ`，Render 設定的 Environment Variables 會自動注入到程式環境，**不需要 .env 檔、也不靠 dotenv 套件**。所以上 Render 後 .env 完全用不到，連線資訊全靠這張表。

### 帳號密碼怎麼處理（重要釐清）

5 個角色帳號（owner / staff / warehouse / accounting / viewer）**不是用環境變數**設定的，
而是寫死在 `init_db.py` 的 `SEED_USERS`（預設密碼如 `owner123`、`staff123`…）。
帳號是在「初始化資料庫」時寫進 Postgres 的，不是 Render env。

→ 因此 Render 環境變數**沒有** owner/staff 帳密的 key，不用填。

> 安全建議（部署後）：預設密碼太弱，正式上線前應改密碼（改 `init_db.py` 的 SEED_USERS 後重跑，或日後做後台改密功能）。此項先記著，由森哥決定何時處理。

---

## 步驟 3：初始化線上資料庫（第一次部署後做一次）

Postgres 是空的，要先建表 + 灌入種子資料（帳號、商品、庫存）。
在 Render → 你的 service → Shell（免費版有提供）執行一次：

```
python init_db.py
```

跑完會印出「tables created (20)」與「seed done」。之後就能用帳號登入。
（`init_db.py` 是冪等的，重複跑不會重複建資料，但仍建議只在初始化時跑。）

---

## 步驟 4：驗證

- 開 `https://你的服務.onrender.com/health` → 應回 `{"status":"ok","db":"postgres","schema":"flora_court"}`
- 開 `/login` → 用 `owner` / `owner123` 登入測試

---

## ⚠️ 免費版冷啟動提醒

Render Free 方案的服務閒置約 15 分鐘後會「睡著」。下次有人開網頁時要重新喚醒，
**第一次載入會慢約 30～60 秒**（之後就正常）。這是免費版正常現象，不是壞掉。
若日後要避免冷啟動，需升級到付費方案（這屬「涉及費用」決策，要先跟森哥確認，不自行升級）。
