# Flora Court 面膜 ERP — 啟動說明（地基層）

獨立 Flask app，獨立 schema `flora_court`（R2）。本機測試走 SQLite，正式走 Postgres。

## 本機 SQLite 跑法（最快驗證地基）

```bash
cd E:/AI-Wiki/Projects/flora_court
python -m venv .venv
source .venv/Scripts/activate    # Windows Git Bash
# 或 PowerShell: .venv\Scripts\Activate.ps1
pip install -r requirements.txt

# 1) 建表 + 冪等 seed（SQLite，schema=None）
python init_db.py

# 2) 啟動
python app.py
# 開 http://localhost:5000/login
```

預設帳號（seed）：
- owner / owner123（老闆，最高權限）
- staff / staff123（員工）

驗證：
- `GET /health` → 回 `{"status":"ok","schema":null,"db":"sqlite",...}`
- 登入後導覽列 6 區（商品/庫存/訂單/客戶/報表/手機版）+ 登出皆可點

## 正式 Postgres 跑法（schema=flora_court）

```bash
export DATABASE_URL="postgresql://user:pass@host:5432/dbname"
export DB_SCHEMA="flora_court"        # 預設即 flora_court，可省
export SECRET_KEY="...正式密鑰..."
export ENABLE_EARWAX_ENTRY=false      # D1：愛啪啪入口隱藏
python init_db.py                     # 自動 CREATE SCHEMA IF NOT EXISTS flora_court
gunicorn -w 2 -b 0.0.0.0:5000 app:app
```

## 紅線提醒
- R1：改庫存量只能經 `inventory_service.py` 契約函式，在同一 transaction 內寫 `inventory_movements`。禁直接 UPDATE `inventory_balances`。
- R2：本 app 不 import 任何 earwax model；FK 全落 flora_court 內；SQLite 測試 schema=None、Postgres schema=flora_court。

## 環境變數一覽
| 變數 | 預設 | 說明 |
|------|------|------|
| DATABASE_URL | sqlite:///flora_court.db | 切 Postgres 即啟用 flora_court schema |
| DB_SCHEMA | flora_court（非 sqlite 時） | schema 名 |
| SECRET_KEY | dev 預設 | 正式務必改 |
| ENABLE_EARWAX_ENTRY | false | D1 愛啪啪入口可逆隱藏 |
| ENABLE_MOBILE | true | 手機後台 /m |
| CONCURRENCY_STRATEGY | sqlite=atomic_update / pg=for_update | §4.3 併發策略 |
