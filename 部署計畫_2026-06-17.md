# Flora Court 面膜 ERP — 部署上線計畫
日期：2026-06-17　前置：本機 DONE（閘③ codex 認證）
原則：每步需森哥授權才執行；涉費用/線上 DB/付費設定＝重大決策

## D-1 GitHub（程式上版）
- flora_court 是獨立 app。選項：①新建 repo `flora-court`（乾淨，建議）②推到現有 earwax-hualin repo 子目錄
- 動作：git init + remote + push（push 行為森哥先前已授權）
- .env / *.db 不上傳（已 .gitignore）

## D-2 線上資料庫（正式 Postgres）⚠️ 舊卡點所在
- 用同一顆 MUSEN-SAAS Supabase（與泰安/earwax 同 instance），連線走 Pooler(IPv4)
- 建 `flora_court` schema + 20 表 + seed（create_all + init_db.py 冪等 seed）
- seed：boxed/normal=1271、boxed/reserved=35、loose/normal=1825、paper_bag/normal=932（後台可編輯）
- 風險：寫正式 DB（不可逆）；但獨立 schema，泰安/earwax 零影響（撞表=0 設計）
- **需森哥授權**：之前卡在「只授權 push/deploy、未授權直接操作線上 DB」
  - 選項：(A) 授權 AI 直接對線上 DB 跑 init_db (B) 森哥自己在視窗跑（AI 給確切指令）(C) Supabase 網頁介面手動

## D-3 Render 服務（費用）
- 開獨立 Flora Court Web Service
- Env：DATABASE_URL(pooler)、SECRET_KEY、SCHEMA=flora_court、owner/staff 密碼、ENABLE flags
- 費用：免費版（會冷啟動、首次開慢）vs Starter ≈ $7/月（常駐免冷啟）
- Render 服務建立在森哥帳號（瀏覽器操作），AI 提供確切 env 值

## D-4 正式套用「隱藏愛啪啪」
- earwax Render service 設 `ENABLE_EARWAX_ENTRY=false` → 重新部署
- 可逆（設回 true 還原 15 路由）

## D-5 上線驗證
- 線上 smoke：/health、登入、各頁 200、staff 擋後台
- 正式撞表掃描：連正式 DB 實證 earwax/泰安 零影響（撞表=0）
- 通過 → D17 部署部分 DONE，全案 DONE

## 森哥裁決（已定 2026-06-17）
1. 線上 DB：授權 AI 直接跑 → ✅ D-2 完成（flora_court schema 20表+seed，撞表=0）
2. Render：免費版
3. GitHub：推現有 earwax repo → ✅ D-1 完成（推到 flora-court 分支，earwax main 未動，commit 1459e0c）
4. 部署拓撲＝**甲：repoint 現有 earwax Render 網站 → flora-court 分支 + flora_court DB**（客戶沒在用 earwax 站、可安全換）
   - 同網址變面膜 ERP；愛啪啪程式+資料保留、改回 main 即還原（可逆）
   - **D-4 隱藏愛啪啪由此自動達成**（不需動 earwax main 程式）
5. 密碼：弱預設先不改，列為**上線前待辦風險**（owner/owner123 等）

## 剩餘步驟
- D-3'（森哥瀏覽器）：改現有 Render 服務 Branch=flora-court、Start=gunicorn app:app、Env 加 DB_SCHEMA=flora_court + ENABLE_MOBILE=true（DATABASE_URL/SECRET_KEY 沿用）→ 重佈 → 給網址
- D-5（AI）：上線驗證（/health、登入、各頁、再掃撞表）
- 上線後待辦：改強密碼
